[pytest]
# Default patterns are test_*.py and *_test.py only — this repo uses testing_pipeline_accuracy.py.
python_files = test_*.py *_test.py testing_*.py
markers =
    integration: slower tests that need data or GPU (optional)
